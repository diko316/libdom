'use strict';


describe(`Dispatches custom event [type] to browser [observable] using
        dispatch(observable:Observable,
                type:String,
                `,
    () => {
        
    });